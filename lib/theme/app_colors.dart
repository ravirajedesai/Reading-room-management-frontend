import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // =========================================================
  // PRIMARY
  // =========================================================

  static const Color primary = Color(0xFF4054C7);
  static const Color primaryDark = Color(0xFF26388F);
  static const Color primaryLight = Color(0xFF6575D6);
  static const Color primarySoft = Color(0xFFEFF1FF);

  // =========================================================
  // BACKGROUND
  // =========================================================

  static const Color background = Color(0xFFF3F5FF);
  static const Color sectionBackground = Color(0xFFE9EDFF);
  static const Color surface = Colors.white;

  // =========================================================
  // TEXT
  // =========================================================

  static const Color textPrimary = Color(0xFF172033);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);

  // =========================================================
  // STATUS
  // =========================================================

  static const Color success = Color(0xFF159570);
  static const Color successLight = Color(0xFFE8F8F2);

  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFFF7E6);

  static const Color error = Color(0xFFE53935);
  static const Color errorLight = Color(0xFFFFEEEE);

  static const Color info = Color(0xFF2196F3);
  static const Color infoLight = Color(0xFFEAF4FF);

  // =========================================================
  // BORDER
  // =========================================================

  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF0F1F5);

  // =========================================================
  // SEAT STATUS
  // =========================================================

  static const Color seatAvailable = Color(0xFF159570);
  static const Color seatBooked = Color(0xFFE53935);
  static const Color seatPending = Color(0xFFF59E0B);

  // =========================================================
  // GRADIENT
  // =========================================================

  static const Color gradientStart = Color(0xFF4054C7);
  static const Color gradientEnd = Color(0xFF6575D6);
}
